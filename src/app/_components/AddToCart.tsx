'use client';
import React from "react";

const AddToCart = () => {
    return (
        <button className='btn btn-primary' onClick={() => alert("Click!")}>Add to cart</button>
    )
}

export default AddToCart